模块以 MainControlProcessModule 为进程入口，继承 ProcessModuleBase 并在 Init/ReOpen/LoadSetting/Save 生命周期中加载 MainControlGlobalSetting（AppParam\ProcessModule\MainControl）与 MainControlProjectSetting（随项目路径切换）。业务核心 XyzControllerHub 与三个 AxisJogService 由模组统一创建，同时注入到 RunForm 与 UnifiedRunForm 两个界面，二者共享同一 Hub 实例实现状态同步；AxisLimitForm 作为设置弹窗修改 globalSetting/projectSetting 后调用 ApplyRanges() 将范围回写到 Hub 和已打开的界面。Logic/Controls 子目录分别承载运动控制逻辑与可复用控件，Resources 存放资源文件，顶层保留与 Controls 同名的 WinForms 类供界面直接引用。