.NET Framework 4.6.1 + WinForms；使用 `System.Drawing` 与 `System.Drawing.Drawing2D` 自定义绘制 XY/Z 视图；XML 序列化通过 `InterfaceDefine.CommKit.XMLSerializationHelper`；MSBuild 项目文件通过 `<Compile Include="Logic\*.cs" />` 直接编译共享逻辑代码，资源通过 `<EmbeddedResource Include="Resources\*.bmp" />` 嵌入。