构建输出 `Trajectory.dll`，命名空间 `TrajectoryViewProcessModule`，类 `TrajectoryViewProcessModule`。需引用 `InterfaceDefine`、`MainModule`、`ProcessModules.Common` 三个外部程序集；Logic 目录下的 `.cs` 文件通过 `<Compile Include>` 直接编译而非链接，资源文件通过 `<EmbeddedResource>` 嵌入到 DLL 中。