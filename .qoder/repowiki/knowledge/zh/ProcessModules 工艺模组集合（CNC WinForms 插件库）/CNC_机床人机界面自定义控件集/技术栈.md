C# WinForms + System.Drawing（非 SkiaSharp/GDI+ 之外的第三方库），使用 .NET Framework 的 `System.ComponentModel` 设计器属性系统暴露属性，`System.Windows.Forms.Timer` 驱动重复触发（JogButton 与 DroLabel 的闪烁效果），注释中明确兼容“工控机旧编译器不支持 Lambda”的环境。