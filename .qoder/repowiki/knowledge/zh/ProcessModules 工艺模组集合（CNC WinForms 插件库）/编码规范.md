- 每个工艺模组采用 GlobalSetting/ProjectSetting/ProcessModule + RunForm 的四件套结构，命名空间按模组名划分（MainControl、PointJump、Trajectory）。
- 所有可序列化的配置类使用公共字段而非属性，以便 System.Xml 直接序列化。
- RunForm 与其 Designer.cs、resx 资源文件成对出现，并通过 DependentUpon 关联。
- 运动控制统一通过 XyzControllerHub 暴露命令，避免各模组直接依赖底层轴控制器。