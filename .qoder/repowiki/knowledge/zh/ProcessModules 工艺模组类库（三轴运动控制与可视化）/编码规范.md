- 每个工艺模块必须直接继承外部 InterfaceDefine.dll 中的 ProcessModuleBase，禁止创建中间扩展基类
- 命名空间与类名必须等于模块名加 ProcessModule 后缀，以匹配平台反射加载约定
- 每个模块保持标准组成：*ProcessModule.cs、*GlobalSetting.cs、*ProjectSetting.cs、RunForm.cs 四个文件
- 工控机编译器不支持 Lambda，统一使用命名方法 + 委托构造
- 各模块间完全解耦，禁止共享状态与相互依赖，设置与界面各自独立实现