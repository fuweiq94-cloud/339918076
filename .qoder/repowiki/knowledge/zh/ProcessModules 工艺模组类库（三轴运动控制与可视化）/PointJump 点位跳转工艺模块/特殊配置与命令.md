编译目标为 AnyCPU 的 DLL 库，需确保上位机平台已提供 InterfaceDefine.dll 和 MainModule.dll 引用；模块初始化时 `Init("PointJump")` 中的名称必须与文件名一致；配置文件默认路径为 `./Config/PointJump_GlobalSettings.json` 与 `./Config/PointJump_ProjectSettings.json`（README 描述），实际代码使用 XML 序列化到 AppParam 和项目目录下。